# secrets — never commit the real .env
.env

# Terraform
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# vocabulary CSVs (large binary/text downloads; not source-controlled)
athena_vocab/
*.zip

# DuckDB databases
*.duckdb
*.wal

# data
*.parquet
*.csv
*.json
*.xlsx

# lance data store
*.lance

# Claude Code shared config (override the broad *.json data ignore above so the
# session-start hook is registered for everyone). Local overrides stay ignored.
!.claude/settings.json

# Python
__pycache__/
*.py[cod]
.venv/
dist/
*.egg-info/
.mypy_cache/
.ruff_cache/

# pytest / coverage
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# logs
*.logs
logs/

# OS
.DS_Store
Thumbs.db

# Other
*.tmp